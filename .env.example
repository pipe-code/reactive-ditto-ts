# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real keys.

# URL for the third-party form proxy endpoint (POST /wp-json/proxy/v1/submission).
# Leave empty to disable the proxy endpoint.
PROXY_SUBMISSION_URL=
